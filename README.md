As requested a makefile you my good tutor.

My code just uses a makefile to compile. Type: "make" - to compile, "make run" - to run.
If you want to edit the parameters (no of words falling and on screen and the text file that is read in) go to the make run line in the makefile and edit them there.
